apiVersion: tekton.dev/v1beta1
kind: PipelineRun
metadata:
  name: tax-pipeline-run
spec:
  pipelineRef:
    name: tax-pipeline
  params:
    - name: image-tag
      value: "us.icr.io/tu-namespace/tax-calculator:latest"
  workspaces:
    - name: shared-workspace
      persistentVolumeClaim:
        claimName: shared-pvc
